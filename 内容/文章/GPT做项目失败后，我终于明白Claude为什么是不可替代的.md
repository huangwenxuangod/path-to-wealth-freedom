# GPT做项目失败后，我终于明白Claude为什么是不可替代的

**那天我对着 GPT 的输出发呆，屏幕上是一堆漂亮但完全没用的废话。**

我想做一个定时日报项目，需求很明确：每天固定时间自动抓取我的工作记录，整理成日报，然后发给指定邮箱。

GPT 答应得特别痛快，「没问题！这个简单！」，然后给我列了一堆技术栈，写了一段看起来很美的代码。

结果一运行，全是错的。

---

## 我用 GPT 做定时日报项目，失败了

**我那天才意识到，GPT 更像一个只会说漂亮话的销售，而不是能帮你解决问题的工程师。**

事情是这样的：我需要一个定时日报工具，已经想清楚了大概的逻辑——用 GitHub Actions 定时运行，读取我 Obsidian 里的每日笔记，提取关键内容，整理成 Markdown，然后用邮件发出去。

我把这个需求告诉 GPT，它的第一反应是：「太棒了！这个想法太实用了！我来帮你实现！」

然后它开始输出：
- 先给我列了 5 个「核心优势」
- 又给我画了一个「系统架构图」（用文字描述的）
- 接着写了一段 Python 代码，看起来很专业
- 最后告诉我「这样就搞定了！」

我满心欢喜地复制代码去运行，结果报错。我把错误信息发给它，它说「哎呀，这个小问题，我来修！」，然后又给我一段新代码。

就这样反复了 5 次，每次都有新的错误。

最后我放弃了。

---

## 换 Claude 试试，结果完全不一样

**我本来没抱希望，只是想着换个模型试试，结果 Claude 的第一句话就让我愣住了。**

我把同样的需求发给 Claude，甚至连措辞都没改。

Claude 没有说「太棒了」，也没有立刻给我写代码。

它的第一句话是：「在开始之前，我需要先确认几个细节，这样才能给你一个真正能用的方案。」

然后它问了我 7 个问题：
1. 你的 Obsidian 每日笔记是放在哪个文件夹？文件名格式是什么？
2. 你想提取笔记里的哪些内容？是全部内容，还是有特定的标题或标签？
3. 定时运行的频率是怎样的？每天几点？
4. 邮件发给谁？用什么邮箱发？
5. 日报的格式有什么要求吗？有没有模板？
6. 如果某天没有笔记，是跳过还是发空的？
7. 你对技术栈有偏好吗？还是我来选最合适的？

我一个一个回答完，大概花了 3 分钟。

然后 Claude 说：「好的，现在我清楚了。我给你一个完整的方案，包括代码、配置、以及每一步怎么部署。」

它给我的东西：
- 一个完整的 Python 脚本，注释清晰
- GitHub Actions 的配置文件，直接能用
- 每一步的部署说明，从创建仓库到设置 Secrets
- 几个常见问题的解决方案
- 如果出了问题，怎么排查

我照着做，一次就成了。

那天我盯着屏幕看了很久，第一次觉得：**这才是 AI 该有的样子。**

---

## 这不是偶然，是底层逻辑的区别

**那天的对比太强烈了，我开始思考：为什么两个模型的表现会差这么多？**

不是 GPT 能力不行，是它们的**底层逻辑不一样**。

GPT 想的是：「怎么让用户觉得我很厉害？怎么让用户开心？」
所以它先赞美你，给你看漂亮的架构图，说一堆你想听的话。至于最后能不能成，那是次要的。

Claude 想的是：「怎么帮用户把这件事做成？」
所以它先问清楚细节，补全你没说出来的信息，然后给你一个真正能落地的方案。

一个是「表演型」，一个是「做事型」。

这就是核心区别。

---

## 测试场景：我要做一个定时日报项目

**为了让你直观感受到三个模型的区别，我把同样的需求分别发给了它们，结果一目了然。**

需求原文是这样的：
> 我想做一个定时日报项目，每天固定时间自动抓取我的 Obsidian 每日笔记，整理成日报，然后发邮件给指定邮箱。帮我实现一下。

就这么简单，没有更多细节。

我想看看，三个模型分别会怎么反应。

---

## GPT 的表现：过度谄媚，一堆废话，一到执行就拉胯

**GPT 的回答让我感觉像是在跟一个餐厅服务员说话，而不是在跟一个工程师讨论技术问题。**

它的第一句话是：「这是一个非常棒的想法！自动化日报可以帮你节省大量时间，让你更专注于工作本身！我完全理解你的需求，这就来帮你实现！」

然后它开始输出：

### 第一步：给我画饼
- 「这个系统有以下 5 个核心优势……」
- 「它将彻底改变你的工作流程……」
- 「你会爱上这种自动化的感觉！」

### 第二步：给我看「架构图」（用文字描述的）
- 「系统分为三层：数据层、处理层、通知层……」
- 「每个层之间通过解耦的方式连接……」
- 「这样设计的好处是扩展性极强……」

### 第三步：写一段看起来很美的代码
```python
import os
import smtplib
from datetime import datetime
from email.mime.text import MIMEText

class DailyReportSystem:
    def __init__(self):
        self.config = self.load_config()

    def load_config(self):
        # 加载配置
        return {}

    def fetch_notes(self):
        # 获取笔记
        pass

    def generate_report(self):
        # 生成日报
        pass

    def send_email(self):
        # 发送邮件
        pass

    def run(self):
        notes = self.fetch_notes()
        report = self.generate_report(notes)
        self.send_email(report)

if __name__ == "__main__":
    system = DailyReportSystem()
    system.run()
```

这段代码写得很漂亮，有类，有方法，有注释——但就是**什么都没做**。

所有的核心方法都是 `pass`。

### 第四步：告诉我「这样就搞定了！」
- 「把这段代码复制过去，稍微修改一下配置就可以用了！」
- 「有问题随时问我！」

我复制过去，运行，报错。我把错误发给它，它说「哎呀，这个小问题，我来修！」，然后又给我一段新代码。

反复 5 次之后，我放弃了。

**GPT 不是在帮我解决问题，它是在表演「帮我解决问题」。**

---

## Gemini 的表现：中庸，但同样在关键环节掉链子

**Gemini 的表现比 GPT 稍微务实一点，但也仅此而已。**

它没有说那么多漂亮话，直接开始给方案。

但是：
- 它假设我的 Obsidian 笔记在 `~/Documents/Obsidian/Notes/` 文件夹
- 它假设文件名格式是 `YYYY-MM-DD.md`
- 它假设我想提取整个文件的内容
- 它假设我用 Gmail 发邮件
- 它假设邮件发给自己

这些假设**没有一个是对的**。

我把这些问题告诉它，它说「好的，那你修改一下这些变量就可以了」，然后给我列了一堆变量让我自己改。

问题是，我不知道有些值应该怎么填。比如 GitHub Actions 里的 Secrets 怎么设置？邮件服务器的端口是多少？

我继续问，它的回答就开始模糊了：「这个你可以去查一下文档」、「不同的邮箱提供商可能不一样」。

最后我还是没搞定。

**Gemini 像是一个刚入行的实习生，能干活，但需要你把每一步都告诉他，而且关键时刻顶不上去。**

---

## Claude 的表现：先问问题，补全细节，直接搞定

**Claude 的第一句话就让我知道，这个模型不一样。**

它没有赞美，没有画饼，直接问：「在开始之前，我需要先确认几个细节，这样才能给你一个真正能用的方案。」

然后它问了 7 个问题：

1. 「你的 Obsidian 每日笔记是放在哪个文件夹？文件名格式是什么？」
2. 「你想提取笔记里的哪些内容？是全部内容，还是有特定的标题或标签？」
3. 「定时运行的频率是怎样的？每天几点？」
4. 「邮件发给谁？用什么邮箱发？」
5. 「日报的格式有什么要求吗？有没有模板？」
6. 「如果某天没有笔记，是跳过还是发空的？」
7. 「你对技术栈有偏好吗？还是我来选最合适的？」

我一个一个回答：
- 文件夹是 `D:/Obsidian/Vault/每日笔记/`，文件名格式是 `YYYY年MM月DD日.md`
- 只提取标题为「今日完成」和「明日计划」的部分
- 每天晚上 22:00 运行
- 用我的 163 邮箱发，发给我的 Gmail
- 格式就是简单的 Markdown，不需要太复杂
- 如果没有笔记，就发「今日无记录」
- 技术栈你选，简单稳定就行

回答完之后，Claude 说：「好的，现在我清楚了。我给你一个完整的方案，直接照着做就能用。」

然后它给了我三样东西：

### 第一样：完整的 Python 脚本
```python
import os
import re
import smtplib
from datetime import datetime, timedelta
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart

# 配置
VAULT_PATH = "D:/Obsidian/Vault/每日笔记/"
EMAIL_FROM = "your-email@163.com"
EMAIL_TO = "your-email@gmail.com"
EMAIL_PASSWORD = "your-email-password"  # 163邮箱用授权码

def get_note_path():
    """获取今天的笔记文件路径"""
    today = datetime.now()
    filename = f"{today.year}年{today.month:02d}月{today.day:02d}日.md"
    return os.path.join(VAULT_PATH, filename)

def extract_content(note_path):
    """提取笔记中的特定内容"""
    if not os.path.exists(note_path):
        return "今日无记录"

    with open(note_path, "r", encoding="utf-8") as f:
        content = f.read()

    # 提取"今日完成"和"明日计划"部分
    result = []
    sections = ["今日完成", "明日计划"]

    for section in sections:
        # 匹配标题及其后的内容，直到下一个标题或文件结束
        pattern = rf"#+\s*{section}\s*\n(.*?)(?=\n#+|$)"
        match = re.search(pattern, content, re.DOTALL)
        if match:
            result.append(f"## {section}\n{match.group(1).strip()}")

    return "\n\n".join(result) if result else "今日无记录"

def send_email(subject, body):
    """发送邮件"""
    msg = MIMEMultipart()
    msg["From"] = EMAIL_FROM
    msg["To"] = EMAIL_TO
    msg["Subject"] = subject

    msg.attach(MIMEText(body, "plain", "utf-8"))

    # 163邮箱SMTP服务器
    with smtplib.SMTP_SSL("smtp.163.com", 465) as server:
        server.login(EMAIL_FROM, EMAIL_PASSWORD)
        server.send_message(msg)

def main():
    note_path = get_note_path()
    content = extract_content(note_path)

    today = datetime.now().strftime("%Y年%m月%d日")
    subject = f"日报 - {today}"

    send_email(subject, content)
    print(f"日报发送成功: {subject}")

if __name__ == "__main__":
    main()
```

这段代码是**真的能运行**的，每一个环节都写死了，没有 `pass`。

### 第二样：GitHub Actions 配置文件
```yaml
name: Daily Report

on:
  schedule:
    - cron: '0 14 * * *'  # UTC时间14:00 = 北京时间22:00

jobs:
  send-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Create note directory
        run: mkdir -p ./daily-notes

      - name: Run script
        env:
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: python send_report.py
```

连 cron 表达式都帮我算好了：UTC 14:00 = 北京时间 22:00。

### 第三样：保姆级部署说明
1. 创建一个新的 GitHub 仓库
2. 把上面的 Python 脚本保存为 `send_report.py` 放进去
3. 把 GitHub Actions 配置保存为 `.github/workflows/daily-report.yml` 放进去
4. 在仓库的 Settings → Secrets and variables → Actions 里添加三个 Secrets：
   - `EMAIL_FROM`: 你的 163 邮箱
   - `EMAIL_TO`: 接收邮箱
   - `EMAIL_PASSWORD`: 163 邮箱的授权码（不是登录密码，去 163 邮箱设置里开 SMTP 服务获取）
5. 把你的 Obsidian 每日笔记同步到这个仓库的 `daily-notes/` 文件夹
6. 提交推送，搞定

它甚至还加了一条：「如果你想测试，可以手动在 Actions 页面点击 Run workflow，不用等到 22:00。」

我照着做，一次就成了。

**那天我第一次觉得：AI 真的能帮我做事。**

---

## 对比表格：三个模型的完整对话记录

**为了让你更直观地对比，我把三个模型的对话精简成了表格。**

| 维度 | GPT | Gemini | Claude |
|------|-----|--------|--------|
| **第一反应** | 「太棒了！这个想法太实用了！」 | 直接开始写代码 | 「在开始之前，我需要先确认几个细节」 |
| **提问次数** | 0 次 | 0 次 | 7 次 |
| **废话比例** | 60% | 30% | 5% |
| **代码质量** | 漂亮但不能用 | 能用但需要改很多 | 直接能用 |
| **部署说明** | 「稍微修改一下配置」 | 「去查一下文档」 | 保姆级，每一步都写清楚 |
| **是否一次成功** | 否，反复 5 次还是失败 | 否，最后没搞定 | 是，一次就成 |
| **整体感受** | 像在跟销售说话 | 像在跟实习生说话 | 像在跟靠谱的工程师说话 |

这个对比说明了一切。

---

## 核心差异 1：Claude 是「工程师思维」，GPT/Gemini 是「销售思维」

**这是最根本的区别——它们的目标不一样。**

工程师思维的目标是：**把事做成。**
销售思维的目标是：**让你觉得我很厉害。**

所以 GPT 会：
- 先赞美你，让你开心
- 给你看漂亮的架构图，让你觉得它很专业
- 写漂亮但不能用的代码，让你觉得它能力很强
- 出了问题就说「小问题，我来修」，让你觉得它很负责

至于最后能不能成，那是次要的。

而 Claude 会：
- 先问清楚细节，确保理解正确
- 不搞花里胡哨的东西，直接给解决方案
- 代码不一定漂亮，但一定能用
- 出了问题会告诉你为什么，以及怎么避免

**一个在乎「表演」，一个在乎「结果」。**

这就是为什么 Claude 能搞定，而 GPT/Gemini 搞不定。

---

## 核心差异 2：先问清楚再动手，而不是上来就瞎猜

**很多时候，你以为自己说清楚了，其实没有。**

我那个定时日报的需求，听起来很明确：「每天固定时间自动抓取我的 Obsidian 每日笔记，整理成日报，然后发邮件给指定邮箱。」

但这里面有太多没说出来的细节：
- 笔记放在哪个文件夹？
- 文件名格式是什么？
- 提取哪些内容？
- 用什么邮箱发？
- 发邮件的密码是什么？
- 没有笔记怎么办？

GPT 和 Gemini 选择了「猜」——猜一个最常见的配置，然后开始写代码。

但问题是，**每个人的使用习惯都不一样**。我的笔记文件名是中文格式的（`YYYY年MM月DD日.md`），不是英文的（`YYYY-MM-DD.md`）；我用 163 邮箱，不是 Gmail。

它们猜的每一个假设，对我来说都是错的。

而 Claude 选择了「问」——把所有不确定的地方都问清楚，确认之后再动手。

这看起来慢，其实是最快的方式。

**先花 3 分钟问清楚，比花 1 小时反复修改强得多。**

---

## 核心差异 3：执行层碾压——真的能把事情做成

**这是 Claude 最可怕的地方：它不只停留在「想法」层面，它真的能把事情落地。**

很多模型都能给你一个「大概的方案」，但 Claude 能给你一个「直接能用的方案」。

什么意思？

就是：
- 代码里的每一个路径都是对的
- 每一个配置项都告诉你怎么填
- 每一个可能出错的地方都提前告诉你
- 甚至连测试方法都帮你想好了

你不需要是一个专家，你只需要照着做，就能成。

这一点太难得了。

**大多数 AI 都停留在「顾问」层面——告诉你应该怎么做，但自己不会动手。**

**而 Claude 是「执行者」——不仅告诉你怎么做，还把所有脏活累活都帮你干了。**

---

## 核心差异 4：废话少，直接给结果

**GPT 和 Gemini 的输出里，有 50% 以上都是废话。**

什么是废话？
- 「这是一个非常棒的想法！」
- 「这样做有很多好处！」
- 「让我来帮你实现！」
- 「这样就搞定了！」

这些话听着舒服，但对解决问题没有任何帮助。

而 Claude 的输出里，几乎没有废话。

它不会赞美你，不会给你画饼，不会说漂亮话。

它只会说：
- 「我需要确认几个细节……」
- 「这是完整的方案……」
- 「照着这几步做……」
- 「如果出了问题，这样排查……」

**简洁、直接、有用。**

这才是 AI 该有的样子。

---

## 核心差异 5：上下文理解深度不在一个级别

**这一点可能是最致命的——Claude 真的能「理解」你在说什么，而不只是「匹配」关键词。**

举个例子：我在说「定时日报」的时候，我其实是在说：
- 我用 Obsidian 记笔记
- 笔记有固定的文件夹和命名格式
- 我只需要笔记里的特定部分
- 我要每天自动发邮件
- 我可能不会设置 GitHub Actions
- 我可能不知道邮箱授权码是什么

Claude 能从我的简单需求里，读出这些没说出来的信息。

所以它会问：「你对技术栈有偏好吗？还是我来选最合适的？」——因为它知道我可能不懂技术栈。

所以它会在部署说明里写：「163 邮箱用授权码（不是登录密码，去 163 邮箱设置里开 SMTP 服务获取）」——因为它知道我可能不知道什么是授权码。

所以它会写：「如果你想测试，可以手动在 Actions 页面点击 Run workflow，不用等到 22:00」——因为它知道我可能想立刻测试。

这种「深度理解」，是 GPT 和 Gemini 不具备的。

**它们能听懂你说的话，但它们读不懂你没说出来的话。**

---

## 如果你要写文案、搞创意：GPT 可能还行

**说了这么多 Claude 的好话，不是说 GPT 和 Gemini 一无是处。**

它们也有自己的优势。

GPT 特别适合：
- 写文案、写广告语
- 搞创意、想点子
- 写故事、写小说
- 把干巴巴的内容写得更生动

为什么？因为这些场景不需要「把事做成」，需要的是「说得好听」。

而这正是 GPT 最擅长的。

我有时候会用 GPT 来润色文章标题，或者想一些创意点子——它确实能给我一些惊喜。

但如果你要**真的解决问题**，别用 GPT。

---

## 如果你要快速搜信息、做总结：Gemini 可以用

**Gemini 也有它的位置。**

Google 的搜索基因在 Gemini 身上体现得很明显——它特别适合：
- 快速搜索信息
- 总结新闻、文章
- 回答事实性问题
- 多模态（看图片、看视频）

我有时候会用 Gemini 来快速查一个技术概念，或者总结一篇长文章——它确实比自己读快得多。

但如果你要**做项目、写代码、解决复杂问题**，别用 Gemini。

---

## 但如果你要真的解决问题、做项目、写代码：必须用 Claude

**这就是结论：如果你要真的解决问题，必须用 Claude。**

什么叫「解决问题」？
- 你要做一个项目，从 0 到 1 上线
- 你要写一段代码，能运行，能解决实际问题
- 你要分析一个复杂问题，得到可执行的结论
- 你要做一件事，这件事有明确的成功标准

这些场景下，Claude 是唯一的选择。

不是说 GPT 和 Gemini 能力不行——它们的能力很强，但它们的**目标不对**。

它们的目标是「让你觉得我很厉害」，而不是「帮你把事做成」。

**而 Claude 的目标，从一开始就是「帮你把事做成」。**

这就是为什么 Claude 不可替代。

---

## 我的日常搭配：三个模型各用在什么地方

**最后说一下我现在的日常搭配，供你参考。**

我现在三个模型都在用，但用在不同的场景：

### Claude：解决问题、做项目、写代码
- 做任何需要落地的项目
- 写需要真正运行的代码
- 分析复杂问题，找解决方案
- 任何有明确成功标准的事

### GPT：写文案、搞创意、润色
- 写文章标题、写广告语
- 想创意点子、想选题
- 润色文章，让它更生动
- 写故事、写小说

### Gemini：搜信息、做总结、多模态
- 快速搜索事实性信息
- 总结新闻、长文章
- 看图片、看视频（多模态）
- 查技术概念、查资料

**一句话总结：用 Claude 做事，用 GPT 写文案，用 Gemini 搜信息。**

---

## 立刻去注册 Claude

**如果你还没有 Claude，现在就去注册。**

注册地址：https://claude.ai

不用翻墙，直接就能用（至少目前是这样）。

注册过程很简单：
1. 输入邮箱
2. 点击验证链接
3. 设置密码
4. 开始用

**真的，现在就去。**

---

## 第一次用 Claude 怎么开始

**第一次用 Claude，我建议你从一个小任务开始。**

不要一上来就做很复杂的项目，先试试水，感受一下它的风格。

推荐的第一个任务：
- 「我想写一个简单的 Python 脚本，读取当前目录下所有的 .md 文件，统计每个文件的字数，然后输出一个表格。」
- 「我想整理一下我的文件夹，把所有的图片都移到一个单独的 images 文件夹里，帮我写一个脚本。」
- 「我想把这篇文章翻译成英文，但不要太生硬，要自然一点。」

随便选一个，发给 Claude，看看它的反应。

你会惊讶的。

---

## 怎么跟 Claude 提问效果最好

**跟 Claude 提问，有几个小技巧。**

### 技巧 1：把你的需求说清楚，但不用太细
- 好的提问：「我想做一个定时日报项目，每天固定时间自动抓取我的 Obsidian 每日笔记，整理成日报，然后发邮件给指定邮箱。」
- 不好的提问：「帮我写一个 Python 脚本，用 smtplib 发邮件，读取 D 盘某个文件夹里的文件……」

**把目标说清楚，细节让 Claude 来问你。**

### 技巧 2：告诉 Claude 你的背景
- 好的提问：「我是一个编程小白，想做一个个人博客，越简单越好，不要用太复杂的技术栈。」
- 不好的提问：「帮我做一个个人博客。」

**告诉 Claude 你是谁，它会给你更合适的方案。**

### 技巧 3：如果不满意，直接说
- 好的反馈：「这个方案太复杂了，我想要更简单的。」
- 不好的反馈：「哦，好吧。」

**Claude 会根据你的反馈调整，你越直接，效果越好。**

---

## 避坑：不要用跟 GPT 说话的方式跟 Claude 说话

**这是很多人都会犯的错——用跟 GPT 说话的方式跟 Claude 说话。**

跟 GPT 说话，你可能会：
- 说很多赞美话
- 把需求拆得特别细
- 反复确认它听懂了

但跟 Claude 说话，你不需要这样。

你只需要：
- 直接说需求
- 回答它的问题
- 告诉它你的感受

**Claude 不喜欢废话，你越直接，它越好用。**

---

## 进阶：如何发挥 Claude 的最大威力

**如果你想发挥 Claude 的最大威力，这里有几个进阶技巧。**

### 技巧 1：给它足够的上下文
不要只说一个简单的需求，把背景信息也告诉它：
- 「我现在在做一个 XXX 项目，已经做了 XXX，现在遇到了 XXX 问题，你帮我看看怎么解决。」

上下文越多，Claude 的方案越合适。

### 技巧 2：让它扮演某个角色
- 「你现在是一个资深的前端工程师，有 10 年经验，帮我看看这个代码怎么优化。」
- 「你现在是我的创业导师，帮我分析一下这个项目靠不靠谱。」

角色设定能让 Claude 的输出更聚焦。

### 技巧 3：让它先思考，再回答
- 「你先别急着给答案，先分析一下这个问题有几种解法，每种解法的优缺点是什么，然后再给我推荐一个最合适的。」

让 Claude 先思考，能得到更深入的答案。

---

## 不是说 GPT/Gemini 不好，而是它们的定位不一样

**再强调一遍：不是说 GPT 和 Gemini 不好。**

它们也有自己的优势，也有自己适合的场景：
- GPT 适合写文案、搞创意
- Gemini 适合搜信息、做总结

但如果你要**真的解决问题、做项目、写代码**，Claude 是唯一的选择。

**定位不同而已。**

---

## 选择模型的核心标准：能不能帮你把事做成

**最后，给你一个选择模型的核心标准。**

不要看评测分数，不要看参数多少，不要看谁更火。

只看一点：**它能不能帮你把事做成？**

如果能，就用它；如果不能，就换掉。

**就这么简单。**

---

## GPT 不是在帮你解决问题，它是在表演「帮你解决问题」

**这篇文章写了这么多，其实核心就是这一句话。**

Claude 不可替代，不是因为它技术最强，而是因为它真的想帮你把事做成。

而其他模型，要么在表演，要么在应付。

**希望你也能找到那个「真的帮你把事做成」的工具。**

喜欢这篇文章的可以点点关注哈~
