# 待办池

> **怎么用（30 秒看完）**
> 1. 想到什么随手往下丢，一句话，不用打磨、不用分类、不用排序
> 2. 做完了：把 `- [ ]` 改成 `- [x]`，或直接删掉
> 3. 卡在等别人（工单/审核/客服回复）：改成 `- [w]` —— 早报会跳过它，不排 DDL、不累计天数、不嘲讽。有回音了改回 `- [ ]`
> 4. 想标紧急：在条目前面加 `【急】`
> 5. 想指定死线：在条目后面写 `due:9-10`（月-日）
>
> 每天早上 8:00，机器人把这里没做完的条目捞出来，按「紧迫性 × 重要性」排四象限、逐条定当天 DDL，发到飞书群。
> 长期项目也往这丢 —— 早报会把它拆成「今天的一步」再排进来。

---

## 待办

- [ ] 去图书馆，产出一条基于内容的爆款视频并推送上去（完成标志：视频已发布到对应平台，链接能点开）
- [w] ViberOut 反馈查询：工单已提交，等客服回复（阻塞在外部，不用每天盯）
- [x] 拓宽渠道：完成 GGSel 的护照验证（完成标志：验证通过或被拒，都有明确结果截图）
- [x] 拓宽渠道：完成 WebMoney 的护照验证（完成标志：验证通过或被拒，都有明确结果截图）

---

## 已完成（攒够一批就删）

（做完的条目改成 `- [x]` 后会自动被跳过，不用搬到这里）
